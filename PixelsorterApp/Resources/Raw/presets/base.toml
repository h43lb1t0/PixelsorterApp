[sort_settings]
sort_by = "hue"
direction = "lr"

[masking_options]
use_canny = false
use_subject = false
use_luminance = false

[canny_options]
threshold = 30

[subject_settings]
padding = 15
what_to_sort = "background"

[luminance_options]
threshold = 50
what_to_sort = "normal"

[mask_combination]
mode = "add"