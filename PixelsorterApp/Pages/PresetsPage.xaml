<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="PixelsorterApp.Pages.PresetsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:material="http://schemas.enisn-projects.io/dotnet/maui/uraniumui/material"
    xmlns:properties="clr-namespace:PixelsorterApp.Resources.Languages"
    xmlns:viewModels="clr-namespace:PixelsorterApp.ViewModels"
    x:DataType="viewModels:PresetsPageViewModel">
    <Shell.TitleView>
        <Label Style="{StaticResource TitleViewLabelStyle}" Text="Presets" />
    </Shell.TitleView>
    <ScrollView>
        <VerticalStackLayout Padding="20,20,20,30" Spacing="16">
            <Label Style="{StaticResource CardHeaderLabel}" Text="{x:Static properties:PresetStrings.PresetsPage_Header}" />

            <Border Padding="16" StyleClass="Elevation3">
                <Border.Shadow>
                    <Shadow
                        Brush="{AppThemeBinding Light={StaticResource Black},
                                                Dark={StaticResource White}}"
                        Opacity="0.10"
                        Radius="10"
                        Offset="0,3" />
                </Border.Shadow>
                <VerticalStackLayout Spacing="12">
                    <Label Text="{x:Static properties:PresetStrings.PresetsPage_CreateYourPresetExplenation}" />
                    <Button
                        Command="{Binding ToggleTomlMapVisibilityCommand}"
                        HorizontalOptions="Start"
                        Style="{StaticResource SecondaryActionButtonStyle}"
                        Text="{Binding TomlMapToggleText}" />
                    <Border
                        Padding="10"
                        IsVisible="{Binding IsTomlMapVisible}"
                        Stroke="{AppThemeBinding Light={StaticResource DividerLight},
                                                 Dark={StaticResource DividerDark}}"
                        StrokeShape="RoundRectangle 8"
                        StrokeThickness="1">
                        <Label FontFamily="JetBrainsMonoRegular" Text="{Binding TomlMapString}" />
                    </Border>
                    <Label
                        FontAttributes="Bold"
                        FontSize="13"
                        Text="{x:Static properties:PresetStrings.PresetsPage_PresetEditor_Heading}"
                        TextColor="{AppThemeBinding Light={StaticResource Primary},
                                                    Dark={StaticResource PrimaryDark}}" />
                    <Border
                        Padding="10"
                        Stroke="{AppThemeBinding Light={StaticResource Primary},
                                                 Dark={StaticResource PrimaryDark}}"
                        StrokeShape="RoundRectangle 12"
                        StrokeThickness="1">
                        <Editor
                            AutoSize="TextChanges"
                            FontFamily="JetBrainsMonoRegular"
                            Text="{Binding PresetToml}"
                            VerticalOptions="Start" />
                    </Border>
                    <material:TextField Title="{x:Static properties:PresetStrings.PresetsPage_NewPresetName_Title}" Text="{Binding PresetName}" />
                    <Grid ColumnDefinitions="Auto,*" ColumnSpacing="8">
                        <material:CheckBox
                            Grid.Column="0"
                            IsChecked="{Binding MakeDefaultPreset}"
                            VerticalOptions="Center" />
                        <Label
                            Grid.Column="1"
                            Text="{x:Static properties:PresetStrings.PresetsPage_SetAsDefaultPreset}"
                            VerticalOptions="Center" />
                    </Grid>
                    <Button
                        Command="{Binding SetBaseAsDefaultPresetCommand}"
                        Style="{StaticResource SecondaryActionButtonStyle}"
                        Text="{x:Static properties:PresetStrings.PresetsPage_SetBasePresetAsDefault}" />
                    <Button Command="{Binding SavePresetCommand}" Text="{x:Static properties:PresetStrings.PresetsPage_SavePreset}" />
                    <Label
                        FontSize="13"
                        Text="{Binding SavePresetValidationMessage}"
                        TextColor="{AppThemeBinding Light={StaticResource TextSecondaryLight},
                                                    Dark={StaticResource TextSecondaryDark}}" />
                </VerticalStackLayout>
            </Border>

            <Label Style="{StaticResource CardHeaderLabel}" Text="{x:Static properties:PresetStrings.PresetsPage_AvilablePresets_Header}" />

            <Border Padding="16">
                <Border.Shadow>
                    <Shadow
                        Brush="{AppThemeBinding Light={StaticResource Black},
                                                Dark={StaticResource White}}"
                        Opacity="0.10"
                        Radius="10"
                        Offset="0,3" />
                </Border.Shadow>
                <VerticalStackLayout Spacing="8">
                    <Grid ColumnDefinitions="*,Auto,Auto">
                        <Label
                            Grid.Column="0"
                            FontAttributes="Bold"
                            Text="{x:Static properties:PresetStrings.PresetsPage_Preset}" />
                        <Label
                            Grid.Column="1"
                            Margin="12,0,0,0"
                            FontAttributes="Bold"
                            Text="{x:Static properties:CommonStrings.Edit}" />
                        <Label
                            Grid.Column="2"
                            Margin="12,0,0,0"
                            FontAttributes="Bold"
                            Text="{x:Static properties:CommonStrings.Delete}" />
                    </Grid>

                    <CollectionView ItemsSource="{Binding AvailablePresets}">
                        <CollectionView.ItemTemplate>
                            <DataTemplate x:DataType="viewModels:PresetsPageViewModel+PresetListItem">
                                <Grid Padding="0,6" ColumnDefinitions="*,Auto,Auto">
                                    <Label
                                        Grid.Column="0"
                                        Text="{Binding Name}"
                                        VerticalOptions="Center" />
                                    <Button
                                        Grid.Column="1"
                                        Margin="12,0,0,0"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.EditPresetCommand}"
                                        CommandParameter="{Binding .}"
                                        Style="{StaticResource SecondaryActionButtonStyle}"
                                        Text="{x:Static properties:CommonStrings.Edit}" />
                                    <Button
                                        Grid.Column="2"
                                        Margin="12,0,0,0"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.DeletePresetCommand}"
                                        CommandParameter="{Binding .}"
                                        Style="{StaticResource SecondaryActionButtonStyle}"
                                        Text="{x:Static properties:CommonStrings.Delete}" />
                                </Grid>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </VerticalStackLayout>
            </Border>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>