<?xml version="1.0" encoding="utf-8" ?>
<uxd:PopupResultPage
    x:Class="PixelsorterApp.Popups.ConfirmationPopup"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:uxd="clr-namespace:UXDivers.Popups.Maui;assembly=UXDivers.Popups.Maui"
    x:TypeArguments="x:Boolean"
    AppearingAnimation="{uxd:FadeInPopupAnimation Duration=300,
                                                  Easing=CubicOut}"
    BackgroundColor="{AppThemeBinding Light=#80000000,
                                      Dark=#B3000000}"
    DisappearingAnimation="{uxd:FadeOutPopupAnimation Duration=200,
                                                      Easing=Linear}">

    <Border
        Padding="24"
        HorizontalOptions="Center"
        VerticalOptions="Center"
        WidthRequest="{OnIdiom Phone=300,
                               Default=380}">
        <Border.StrokeShape>
            <RoundRectangle CornerRadius="16" />
        </Border.StrokeShape>
        <Border.Shadow>
            <Shadow
                Brush="{AppThemeBinding Light={StaticResource Black},
                                        Dark={StaticResource Black}}"
                Opacity="{AppThemeBinding Light=0.2,
                                          Dark=0.4}"
                Radius="10"
                Offset="0,4" />
        </Border.Shadow>

        <VerticalStackLayout Spacing="16">
            <Label
                x:Name="TitleLabel"
                FontAttributes="Bold"
                FontSize="18"
                HorizontalTextAlignment="Center"
                Text="Confirm Action"
                TextColor="{AppThemeBinding Light={StaticResource TextPrimaryLight},
                                            Dark={StaticResource TextPrimaryDark}}" />

            <Label
                x:Name="MessageLabel"
                FontSize="14"
                HorizontalTextAlignment="Center"
                TextColor="{AppThemeBinding Light={StaticResource TextSecondaryLight},
                                            Dark={StaticResource TextSecondaryDark}}" />

            <Grid
                x:Name="ButtonLayoutGrid"
                ColumnSpacing="12"
                RowSpacing="12">
                <Button
                    x:Name="CancelButton"
                    Clicked="OnCancelClicked"
                    Style="{StaticResource SecondaryActionButtonStyle}" />

                <Button x:Name="ConfirmButton" Clicked="OnConfirmClicked" />
            </Grid>
        </VerticalStackLayout>
    </Border>
</uxd:PopupResultPage>