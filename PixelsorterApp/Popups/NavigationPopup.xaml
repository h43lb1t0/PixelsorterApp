<?xml version="1.0" encoding="utf-8" ?>
<uxd:PopupResultPage
    x:Class="PixelsorterApp.Popups.NavigationPopup"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:PixelsorterApp"
    xmlns:uxd="clr-namespace:UXDivers.Popups.Maui;assembly=UXDivers.Popups.Maui"
    x:TypeArguments="x:String"
    AppearingAnimation="{uxd:FadeInPopupAnimation Duration=200,
                                                  Easing=CubicOut}"
    BackgroundColor="{AppThemeBinding Light=#80000000,
                                      Dark=#B3000000}"
    CloseWhenBackgroundIsClicked="True"
    DisappearingAnimation="{uxd:FadeOutPopupAnimation Duration=150,
                                                      Easing=CubicIn}">

    <Border
        HorizontalOptions="Center"
        VerticalOptions="Center"
        WidthRequest="{OnIdiom Phone=300,
                               Default=360}">
        <Border.StrokeShape>
            <RoundRectangle CornerRadius="16" />
        </Border.StrokeShape>
        <Border.Shadow>
            <Shadow
                Brush="{AppThemeBinding Light={StaticResource Black},
                                        Dark={StaticResource Black}}"
                Opacity="{AppThemeBinding Light=0.2,
                                          Dark=0.4}"
                Radius="10"
                Offset="0,4" />
        </Border.Shadow>

        <Grid RowDefinitions="Auto,Auto,*">
            <!--  Header  -->
            <Grid
                Grid.Row="0"
                Padding="24,20,24,12"
                ColumnDefinitions="*,Auto">
                <Label
                    x:Name="TitleLabel"
                    Grid.Column="0"
                    FontAttributes="Bold"
                    FontSize="18"
                    TextColor="{AppThemeBinding Light={StaticResource TextPrimaryLight},
                                                Dark={StaticResource TextPrimaryDark}}"
                    VerticalOptions="Center" />

                <Border
                    Grid.Column="1"
                    Padding="4"
                    BackgroundColor="Transparent"
                    StrokeThickness="0"
                    VerticalOptions="Center">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="16" />
                    </Border.StrokeShape>
                    <Border.GestureRecognizers>
                        <TapGestureRecognizer Tapped="CloseButton_Clicked" />
                    </Border.GestureRecognizers>
                    <Label
                        FontFamily="MaterialSymbolsFont"
                        FontSize="22"
                        HorizontalOptions="Center"
                        Text="{x:Static local:MaterialSymbolsFont.Close}"
                        TextColor="{AppThemeBinding Light={StaticResource TextSecondaryLight},
                                                    Dark={StaticResource TextSecondaryDark}}"
                        VerticalOptions="Center" />
                </Border>
            </Grid>

            <!--  Divider  -->
            <BoxView
                Grid.Row="1"
                HeightRequest="1"
                Color="{AppThemeBinding Light={StaticResource DividerLight},
                                        Dark={StaticResource DividerDark}}" />

            <!--  Navigation Items  -->
            <VerticalStackLayout
                x:Name="navItemsContainer"
                Grid.Row="2"
                Padding="0,4,0,8"
                Spacing="0" />
        </Grid>
    </Border>
</uxd:PopupResultPage>